/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/evaluation-liver-resection', // GitHub Pagesのリポジトリ名
  assetPrefix: '/evaluation-liver-resection/', // GitHub Pagesのリポジトリ名
};

export default nextConfig;